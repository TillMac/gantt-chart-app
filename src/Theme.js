import { createTheme } from "@mui/material";

const Theme = createTheme({
  palette: {
    primary: {
      main: '#00000',
    },
    secondary: {
      main: '#6c6c6c',
    },
    other: {
      white: 'white',
      btn: '#5D5FEF'
    },
  },
});

export default Theme;
